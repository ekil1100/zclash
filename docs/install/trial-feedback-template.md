# 试用反馈模板

> 遇到问题时复制本模板填写，发给开发者即可。

---

## 环境信息

- **操作系统**：（例：macOS 15.3 arm64 / Ubuntu 24.04 x86_64）
- **Shell**：（例：bash 5.2 / zsh 5.9）
- **安装目录**：（例：~/.local/bin/zclash）
- **zclash 版本**：（执行 `cat <安装目录>/.zclash_version`）

## 问题描述

- **一句话概括**：

- **严重等级**：
  - [ ] 🔴 阻塞：完全不能用
  - [ ] 🟡 影响：能用但体验差
  - [ ] 🟢 建议：可以更好

## 复现步骤

1. 执行命令：`...`
2. 看到输出：`...`
3. 期望行为：`...`
4. 实际行为：`...`

## 相关输出

```
（粘贴完整的命令输出，特别是 INSTALL_* 字段）
```

## 诊断信息（可选）

```bash
# 执行一键诊断并粘贴输出
bash scripts/install/trial-healthcheck.sh --target-dir <你的安装目录>
```

```
（粘贴诊断输出）
```

---

### 填写示例

**环境信息**
- 操作系统：macOS 15.3 arm64
- Shell：bash 5.2
- 安装目录：/tmp/zclash-bin
- zclash 版本：v0.1.0

**问题描述**
- 一句话概括：upgrade 后 verify 报 binary-missing
- 严重等级：🟡 影响

**复现步骤**
1. `bash scripts/install/oc-run.sh install --target-dir /tmp/zclash-bin`
2. `bash scripts/install/oc-run.sh upgrade --target-dir /tmp/zclash-bin --version v0.2.0`
3. `bash scripts/install/oc-run.sh verify --target-dir /tmp/zclash-bin`
4. 期望：PASS；实际：FAIL，INSTALL_FAILED_STEP=binary-missing
